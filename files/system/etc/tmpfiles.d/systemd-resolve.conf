# Overrides /usr/lib/tmpfiles.d/systemd-resolve.conf (same name): with the stub listener off
# (10-x27-no-stub.conf), /etc/resolv.conf points at resolved's upstream servers instead of
# 127.0.0.53. L! re-creates the link on every boot.
L! /etc/resolv.conf - - - - ../run/systemd/resolve/resolv.conf
